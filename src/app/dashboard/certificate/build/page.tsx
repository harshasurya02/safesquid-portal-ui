import { BuilderContainer } from "@/components/certificate/builder/builder-container";

export default function CertificateBuildPage() {
    return (
        <div className="min-h-[calc(100vh-64px)] overflow-hidden">
            <BuilderContainer />
        </div>
    );
}
